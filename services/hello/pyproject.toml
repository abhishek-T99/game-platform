[tool.ruff]
target-version = "py312"
line-length = 119

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes
    "I",   # isort
    "DJ",  # flake8-django
]
ignore = [
    "E501",  # line too long — handled by ruff-format
]

[tool.ruff.lint.isort]
known-first-party = ["api", "hello"]
